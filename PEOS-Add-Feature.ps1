Write-Host "Adding functions..." -ForegroundColor Green;$p=".\src\services\projectService.js";$f="`nexport const updateProject=(id,newName)=>{const projects=getProjects();const updated=projects.map(p=>p.id===id?{...p,name:newName}:p);localStorage.setItem('peos.projects',JSON.stringify(updated));return updated};export const deleteProject=(id)=>{const projects=getProjects();const updated=projects.filter(p=>p.id!==id);localStorage.setItem('peos.projects',JSON.stringify(updated));return updated}";Add-Content $p $f;Write-Host "Done" -ForegroundColor Yellow
